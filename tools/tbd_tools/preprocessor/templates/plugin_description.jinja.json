{
    "id": "{{ plugin.id }}",
    "isStereo": true,
    "name": "{{ plugin.name }}",
    "hint": "{{ plugin.description }}",
    "params": [
    {%- for property in plugin.properties %}
        "{{ property.name }}": "bar"{{ ", " if not loop.last else "" }} 
    {%- endfor %}
    ]
  }
