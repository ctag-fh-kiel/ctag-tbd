#pragma once

#include "esphome/core/automation.h"
#include <tbd/api/event_index.hpp>


namespace tbd::api {

{% for event in api.events %}
template<typename... Ts> class {{ event.name }}_action : public esphome::Action<Ts...> {
public:
void play(Ts... x) override {
  ::{{ event.full_name }}(x...);
}
};
{% endfor %}

}
