syntax = "proto3";

// NEVER change this
message ApiVersion {
  uint32 version = 1;
  uint32 core_hash = 2;
  uint32 base_hash = 3;
  uint32 api_hash = 4;
};

message DeviceInfo {
  int32 api_version = 1;
  int32 api_hash = 2;
}
